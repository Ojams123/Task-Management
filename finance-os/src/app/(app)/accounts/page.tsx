import { ComingNext } from "@/components/ui/coming-next";

export default function AccountsPage() {
  return (
    <ComingNext
      title="Accounts"
      detail="Full account list with filters, add-account flow, and per-institution grouping lands in the next phase."
    />
  );
}
